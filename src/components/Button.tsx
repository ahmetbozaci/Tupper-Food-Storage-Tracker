import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Button: React.FC = () => {
  return (
    <View>
      <Text>Button: React.FC</Text>
    </View>
  );
};

export default Button;

const styles = StyleSheet.create({});
